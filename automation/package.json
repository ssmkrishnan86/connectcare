{
  "name": "connectcare-automation",
  "version": "1.0.0",
  "private": true,
  "description": "ConnectCare End-to-End Positive Clinical Workflow Automation (API & UI)",
  "scripts": {
    "test": "npm run test:api && npm run test:ui",
    "test:api": "dotnet test ApiE2E/ConnectCare.AutomationTests.csproj",
    "test:ui": "playwright test",
    "test:ui:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^24.13.3",
    "typescript": "^5.7.0"
  }
}
